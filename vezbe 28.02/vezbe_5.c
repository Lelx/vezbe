/*Napisati program koji proverava dali je uneti broj pozitivan, negativan ili 0 i na osnovu toga ispisuje kod*/

#include<stdio.h>
main ()
{
	int n;
	printf("Unesite n: ");
	scanf("%d", &n);
	if(n>0 {
	printf("pozitivan");
	} else {
		prinf("negativan")
	};
	return 0;
	